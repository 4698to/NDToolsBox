 <!-- ToolTip="{Binding HelpUrl,UpdateSourceTrigger=PropertyChanged}"-->

<Button Command ="{Binding OpenItemHelp}" Content="&#xe735;" FontFamily="/TextSearch/Fonts/#iconfont" x:Name="help" 
        
        Click="help_Click"
        Grid.Column="2" Background ="Transparent" />

  FocusManager.FocusedElement="{Binding ElementName=searchTextBox}"



<WrapPanel Orientation="Horizontal" Background="{Binding GetUIColor}" HorizontalAlignment="Center" Margin="0 5 0 3" >

    <ItemsControl ItemsSource="{Binding Path=SolidItems}">

        <ItemsControl.ItemsPanel>

            <ItemsPanelTemplate>

                <WrapPanel Width="Auto" IsItemsHost="True"/>

            </ItemsPanelTemplate>

        </ItemsControl.ItemsPanel>

        <ItemsControl.ItemTemplate>

            <DataTemplate>
                <ToggleButton Width="30" Height="18" Margin="1 1 1 1" Style="{StaticResource ToggleBtnStyle}" Content="{Binding Name}" Click="ToggleButton_Checked"/>
            </DataTemplate>

        </ItemsControl.ItemTemplate>
    </ItemsControl>
</WrapPanel>


<Style x:Key="version" TargetType="{x:Type TextBlock}">
    <Setter Property="FontSize" Value="14"/>
    <Setter Property="Foreground" Value="#999999"/>
    <Setter Property="Margin" Value="5 5 5 5"/>
</Style>

<Style x:Key="settingButton" TargetType="{x:Type Button}">
    <Setter Property="Width" Value="30"/>
    <Setter Property="Height" Value="30" />
    <Setter Property="Margin" Value="0 0 0 0"/>
</Style>

Foreground="{TemplateBinding Foreground}" Background="{TemplateBinding Background}"

#6e80f5

a0a0a0